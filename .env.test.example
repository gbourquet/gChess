# gChess Test Environment Configuration (Render.com)
# These values should be configured in Render dashboard as environment variables
# DO NOT commit actual secrets to version control!

# Environment
ENVIRONMENT=test

# Server Configuration
# Render provides PORT automatically, but defaults to 8080
PORT=8080

# Database Configuration
# Render provides a PostgreSQL database with connection string
# Format: postgresql://user:password@host:port/database
# Note: Render uses 'postgresql://' not 'jdbc:postgresql://'
# You may need to convert the format
DATABASE_URL=<PROVIDED_BY_RENDER>
DATABASE_USER=<PROVIDED_BY_RENDER>
DATABASE_PASSWORD=<PROVIDED_BY_RENDER>
DATABASE_POOL_SIZE=5

# JWT Configuration
# Generate with: openssl rand -base64 32
# IMPORTANT: Use a different secret than production!
JWT_SECRET=<GENERATE_SECURE_SECRET_HERE>
JWT_VALIDITY_MS=86400000

# CORS Configuration
# Allowed origins for test environment
CORS_ORIGINS=https://gchess-test.sur-le-web.fr,http://gchess-test.sur-le-web.fr

# Logging
LOG_LEVEL=INFO