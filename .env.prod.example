# gChess Production Environment Configuration
# Copy to .env.prod on production server and update with actual secrets
# NEVER commit this file with real secrets to version control!

# Environment
ENVIRONMENT=prod

# Server Configuration
PORT=8080

# Database Configuration
# Use strong passwords! Generate with: openssl rand -base64 24
DATABASE_URL=jdbc:postgresql://prod-db:5432/gchess_prod
DATABASE_USER=gchess_prod
DATABASE_PASSWORD=<GENERATE_STRONG_PASSWORD_HERE>
DATABASE_POOL_SIZE=20

# JWT Configuration
# Generate with: openssl rand -base64 32
# CRITICAL: Must be different from test environment!
# Store securely and rotate every 90 days
JWT_SECRET=<GENERATE_256BIT_SECRET_HERE>
JWT_VALIDITY_MS=86400000

# CORS Configuration
# Production domain only (HTTPS enforced by nginx)
CORS_ORIGINS=https://gchess.sur-le-web.fr

# Logging
LOG_LEVEL=WARN

# Monitoring (optional)
# ENABLE_METRICS=true
# SENTRY_DSN=<sentry_project_dsn>