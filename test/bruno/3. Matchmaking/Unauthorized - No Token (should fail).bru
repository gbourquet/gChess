meta {
  name: Unauthorized - No Token (should fail)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/matchmaking/queue
  body: none
  auth: none
}

tests {
  test("Status is 401 (Unauthorized)", function() {
    expect(res.status).to.equal(401);
  });
}
